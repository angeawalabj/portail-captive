[tool.poetry]
name = "captive-portal"
version = "0.1.0"
description = "Captive Portal FastAPI App with Admin UI"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = { extras = ["standard"], version = "^0.27.0" }
sqlalchemy = "^2.0"
psycopg2-binary = "^2.9"
pydantic = "^2.6"
python-jose = "^3.3"
passlib = "^1.7"
alembic = "^1.13"
jinja2 = "^3.1"
httpx = "^0.27"

[tool.poetry.dev-dependencies]
pytest = "^7.4"
pytest-asyncio = "^0.23"
black = "^24.0"
isort = "^5.13"
mypy = "^1.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
